'use strict';

/*
    Not ready to implement
 */

const DB = require('../modules/Database');
const fs = require('fs');
const path = require('path');

// Initialize The ORM
const model = DB.getContext();

exports.registerMethod = function(app) {
    const methodName = [
        'method/user'
    ];
};

module.exports = exports;
